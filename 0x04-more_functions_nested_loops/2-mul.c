#include "main.h"

/**
 * mul - multiplies two integers
 * @a: int type variable
 * @b: int type variable
 * Return: returns the result
 */
int mul(int a, int b)
{
	int result;

	result = a * b;
	return (result);
}

